import wrapper from "./wrapper";
import titleEl from "./title";

document.body.appendChild(wrapper());
document.getElementById("AppWrap").appendChild(titleEl());

//document.body.appendChild(wrapper);
//document.body.appendChild(titleEl);
